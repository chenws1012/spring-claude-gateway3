spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR}:${NACOS_PORT:8848}
        namespace: ${NACOS_NAMESPACE}
      config:
        server-addr: ${NACOS_ADDR}:${NACOS_PORT:8848}
        namespace: ${NACOS_NAMESPACE}
        fileExtension: yaml
  application:
    name: gateway
